@model IEnumerable<Goldfish.Pages.PageNav>

@if (Model.Count() > 0) {
<ul @(Model.First().Level == 1 ? "class=menu" : "")>
	@foreach (var nav in Model) {
		<li>
			<a href="@nav.Permalink">@nav.Title</a>
			@Html.Partial("Partial/Menu", nav.SubPages)
		</li>
	}
</ul>
}